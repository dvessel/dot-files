#compdef extract
#autoload

_arguments \
  {-t,--trash-archive}'[Move extracted archive to the trash.]' \
  "*::archive file:_files -g '(#i)*.(7z|bz2|gz|iso|rar|tar.bz2|tar.gz|tar|tbz|tbz2|tgz|Z|zip)(-.)'" \
    && return 0
